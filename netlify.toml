[build]
  publish = "build/web" # Where Flutter outputs the web build
  command = "flutter build web --release"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200